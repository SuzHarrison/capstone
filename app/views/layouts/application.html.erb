<!DOCTYPE html>
<html>
<head>
  <title>BeaconSeniorResources</title>
  <!-- Compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.7/css/materialize.min.css">

  <!-- Compiled and minified JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.7/js/materialize.min.js"></script>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <!-- <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous"> -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <nav>
    <div class="nav-wrapper  indigo darken-1">
      <a href="#" class="brand-logo">Beacon Senior Resources</a>
      <div class="right hide-on-med-and-down">
        <div style="display: inline-block; line-height: 30px; text-align: right;">
          <% if current_user %>
          <%= current_user.name %><br>
          <%= link_to "Sign out", logout_path, id: "sign_out" %>
          <% else %>
          <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %>
          <% end %>
        </div>
        <% if current_user %>
        <div style="display: inline-block;">
          <%= image_tag current_user.image, class: "picprofile" %>
        </div>
        <%end%>
      </div>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Learn more about Senior Care", learn_path %></li>
        <li><%= link_to "My Saved Providers", user_favorites_path(current_user.id) if current_user %></li>
      </ul>
    </div>
  </nav>

  <% if !flash.empty? %>
   <div class="alert alert-info">
     <% flash.each do |key, msg| %>
       <%= content_tag :div, msg, :id => key %>
     <% end %>
   </div>
 <% end %>

    <%= yield %>
</body>

<!-- <div class="mastfoot">
  <div class="inner">
    <p>© 2016 Beacon Senior Resources ~</p>
  </div>
</div> -->
</html>
